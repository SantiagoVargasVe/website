---
export interface Props {
  title: string;
  id: string;
}

const { title } = Astro.props;
const { id } = Astro.props;
---

<section id={id} class="mt-10 flex flex-col gap-5">
  <h2 class="font-roboto text-2xl font-bold text-emerald-500">
    {title}
  </h2>
  <slot />
</section>
